
<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#"><%= webName %></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#">Link</a>
          </li>
        </ul>
        <% if (!currUser) {%>
        <form class="d-flex" role="search" action="/signup" method="get">
          <button class="btn btn-outline-success" type="submit">SignUp <i class="fa-brands fa-google"></i></button>
        </form>
        <% } else {%>
        <div class="d-flex">
          <form class="d-flex gap-2 align-items-center" role="search" action="/logout" method="get">
            <p class="mb-0"><%= currUser.displayName %></p>
            <button class="btn btn-outline-success" type="submit">Logout</button>
          </form>
        </div>
        <% } %>
      </div>
    </div>
  </nav>